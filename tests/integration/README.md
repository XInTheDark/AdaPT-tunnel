# Integration test plan

This directory is reserved for cross-crate protocol tests once the first implementation slice begins.

Expected early coverage:
- admission handshake transcript tests
- carrier-agnostic logical handshake fixtures
- tunnel packet round-trip tests
- end-to-end localhost harnesses
